/** Height of the fixed Topbar — windows and snap zones never go above this. */
export const TOPBAR_HEIGHT = 28

/** Space reserved at the bottom so cascaded windows don't spawn under the Dock. */
export const DOCK_RESERVE = 100

/** Pointer distance (px) from a screen edge that triggers a snap zone while dragging. */
export const SNAP_EDGE_THRESHOLD = 24

export const MIN_WINDOW_WIDTH = 320
export const MIN_WINDOW_HEIGHT = 200

export const CASCADE_OFFSET = 28
export const CASCADE_MAX_STEPS = 8
